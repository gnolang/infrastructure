apiVersion: v1
kind: Service
metadata:
  labels:
    gno.service: gnoland
  name: tx-indexer
  namespace: gno
spec:
  selector:
    gno.service: tx-indexer
  ports:
  - port: 8546
    targetPort: 8546
    protocol: TCP

